# Placeholders only. Copy to deploy/.env (chmod 600). Never commit real values.
# Generate SLAB_AES_KEY: openssl rand -base64 32

SLAB_AES_KEY=
APP_HOSTNAME=slab.example.local
FRONTEND_PORT=18080
FASTAPI_PORT=18081

# Plan of record: Woo store URL. HTTPS only. Not editable in Settings.
# Server must reject http:// at startup.
WOO_BASE_URL=https://www.whidbeywoodstore.com
