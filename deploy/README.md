# Deploy

Milestone 1 ships as a Docker image that serves the capture app.
Where the container runs is chosen at runtime.

No FastAPI and no Woo client in this milestone.

`docker-compose.yml`, `Caddyfile.fragment`, and `.env.example` describe an older two-service stack. They are not the Milestone 1 contract.
